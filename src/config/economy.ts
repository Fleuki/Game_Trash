import type { MachineKind } from './machines';
import type { MaterialId } from './materials';

/**
 * ВСЕ ЧИСЛА ЗДЕСЬ ЧЕРНОВЫЕ.
 *
 * Взяты из GDD §5 и §10 как есть и не прогонялись через таблицу экономики
 * (PLAN §2.1). Любой вывод вида «на 12-й день должно хватать на второй участок»
 * пока не расчёт, а предположение. Меняются одной правкой этого файла.
 */

/** Стартовый капитал. Хватает на первую линию с ручным столом и немного сверху. */
export const STARTING_MONEY = 1000;

/** Цена за единицу фракции при полной чистоте, ₽. GDD §5. */
export const MATERIAL_PRICE: Record<MaterialId, number> = {
  pet: 3,
  aluminium: 15,
  glass: 5,
  // Органика не продаётся. Компостер сделает из неё компост в S15.
  organic: 0,
  // Новички из GDD §12 — «самые дорогие». Цен в GDD нет, числа придуманы так,
  // чтобы одна пропущенная батарейка стоила дороже десятка бутылок: иначе
  // потеря не чувствуется и вклиниваться в отлаженную линию незачем.
  battery: 40,
  electronics: 60,
};

/** С этой чистоты партия идёт по полной цене. */
export const PURITY_FULL = 0.95;

/** Ниже этого порога партия считается браком. */
export const PURITY_SCRAP = 0.8;

/** Множитель цены для брака. GDD §6. */
export const SCRAP_MULTIPLIER = 0.3;

/** Множитель на пороге брака: между ним и полной ценой идёт прямая. */
export const THRESHOLD_MULTIPLIER = 0.5;

/** Стоимость постройки, ₽. Конвейер и развилка — из GDD §10. */
export const BUILD_COST = {
  belt: 20,
  splitter: 60,
  // Источника и приёмника в таблице GDD нет: числа придуманы, чтобы их нельзя
  // было ставить бесконечно. Кандидаты на пересмотр в первую очередь.
  inlet: 150,
  outlet: 150,
  // Сброс — дырка в земле, дешевле всего прочего.
  waste: 40,
} as const;

/** Стоимость машин, ₽. GDD §10. */
export const MACHINE_COST: Record<MachineKind, number> = {
  manual: 300,
  magnet: 800,
  optical: 2500,
};

/**
 * Сколько возвращается при сносе или замене. GDD §10 обещает 80–90%.
 * Полная перенастройка без сноса — это S17, здесь только возврат денег.
 */
export const REFUND_RATE = 0.85;
