/**
 * Куча отходов. Числа черновые, как и вся экономика.
 */

/** Вывоз, ₽ за единицу. GDD §7. */
export const DISPOSAL_COST_PER_UNIT = 4;

/**
 * Сколько единиц кучи занимает одну клетку площадки.
 *
 * Отсюда берётся «занимает место» из GDD §7: куча не абстрактное число в углу
 * экрана, а растущее пятно, которое отъедает место под линии.
 */
export const PILE_CELL_CAPACITY = 40;
